# WatsonArchiveProjectAutomation

WORK IN PROGRESS 


To run this script, you will need to:
1. Install the geckodriver
2. Install the python dependencies: selenium, dotenv, google-genai
2. Create a .env file with a GEMINI_API_KEY which you can create from Google(free tier is fine) 
3. Run the script
